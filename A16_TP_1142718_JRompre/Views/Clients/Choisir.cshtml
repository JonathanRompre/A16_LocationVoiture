@model IEnumerable<A16_TP_1142718_JRompre.Models.Client>

@{
    ViewData["Title"] = "Choisir";
}

<h1>Choisir</h1>

<p>
    <a asp-action="Create">Create New</a><br />
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prenom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prenom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a asp-controller="Reservations" asp-action="Confirmer" asp-route-id="@item.Id">Edit</a> | 
                @Html.ActionLink("Confirmer","Confirmer","Reservations",new {autoId = ViewBag.autoId, clientId = item.Id})
            </td>
        </tr>
}
    </tbody>
</table>
